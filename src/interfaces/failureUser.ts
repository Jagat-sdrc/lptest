/**
 * This interface will help in getting sync failure user datails
 * @author Ratikanta
 * @since 0.0.1
 * 
 * @interface IFailureUser
 */
interface IFailureUser{
    userId: string;
    reasonOfFailure: string;
}