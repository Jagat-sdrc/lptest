/**
 * This interface will keep the format in which we will send data to Post discharge entry page
 * @author Naseem Akhtar
 * @since 0.0.1
 */
interface IDataForPostDischargePage{
    babyCodeHospital: string;
    babyCode: string;
    menuItemId: number;
}