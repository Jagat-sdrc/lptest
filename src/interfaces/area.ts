/**
 * This is Area interface
 * @author Ratikanta
 * @since 0.0.1 
 * @interface IArea
 */
interface IArea{
    id: number;
    shortName: string;
    areaLevel: number;
    name: string;
    parentAreaId: number;
}