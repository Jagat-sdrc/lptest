/**
 * This interface is going to keep unique patient whole id in string and the generated number
 * @author Ratikanta
 * @since 1.0.0
 * 
 * @interface IUniquePatientId
 */
interface IUniquePatientId{
    id: string;
    idNumber: number;
}